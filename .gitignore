node_modules/
*.log
.DS_Store

# commit message 一時ファイル（git add -A に巻き込まれるのを防ぐ）
# git commit -F 用。複数行メッセージを shell 経由で渡すと壊れるため必ずファイルにする。
*.commitmsg
commitmsg.*
cmsg.*
.tmp-commitmsg
*.commitmsg.txt

# 作業用の翻訳ドラフト（Skill の成果物であって、リポジトリの一部ではない）
# 公開リポジトリには含めない。手元には残る。
邦多利巡礼包宣传视频脚本*.md

